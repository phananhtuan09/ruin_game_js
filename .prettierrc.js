module.exports = {
  // Basic formatting
  printWidth: 80, // Wrap lines at 80 characters
  tabWidth: 2, // Use 2 spaces for indentation
  useTabs: false, // Use spaces instead of tabs
  semi: true, // Add semicolons at the end of statements

  // Quotes
  singleQuote: true, // Use single quotes instead of double quotes
  quoteProps: 'as-needed', // Only quote object properties when necessary

  // Trailing commas
  trailingComma: 'es5', // Add trailing commas where valid in ES5 (objects, arrays)

  // Spacing
  bracketSpacing: true, // Add spaces between brackets in object literals
  bracketSameLine: false, // Put > on the next line in multi-line elements

  // Arrow functions
  arrowParens: 'always', // Always add parentheses around arrow function parameters

  // Line endings
  endOfLine: 'lf', // Use LF line endings (Unix-style)

  // Embedded code formatting
  embeddedLanguageFormatting: 'auto', // Format embedded code automatically

  // Game development specific considerations
  // These settings help maintain readability for game logic
};