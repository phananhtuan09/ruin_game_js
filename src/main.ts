import { Application } from '@/core/Application';

document.addEventListener('DOMContentLoaded', () => {
  console.log('🎮 Ruin Game - Starting...');

  const app = new Application();
  app.start();
});
